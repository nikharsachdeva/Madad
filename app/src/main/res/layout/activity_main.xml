<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="15dp"
    tools:context=".MainActivity">

    <!--    <include-->
    <!--        android:id="@+id/included_toolbar"-->
    <!--        layout="@layout/toolbar_layout"></include>-->

    <TextView
        android:id="@+id/longPressToUnlock"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:gravity="center"
        android:text="@string/long_press_any_item_to_unlock"
        android:textColor="#000000"
        android:textSize="16sp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/saveList_btn"
        android:layout_below="@+id/longPressToUnlock"
        android:layout_marginTop="10dp">


    </androidx.recyclerview.widget.RecyclerView>

    <Button
        android:id="@+id/saveList_btn"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_alignParentBottom="true"
        android:layout_margin="8dp"
        android:background="@drawable/ripple"
        android:text="@string/save_list"
        android:textColor="#000000"
        android:textSize="15sp" />


</RelativeLayout>